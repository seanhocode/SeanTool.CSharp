namespace SeanTool.CSharp.WPFTool
{
    public enum EditorInputType
    {
        Text,
        Boolean,
        Number,
        DateTime,
        Enum,
        FilePath,
        FolderPath,
        Object
    }
}
