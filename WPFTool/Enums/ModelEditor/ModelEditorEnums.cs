namespace SeanTool.CSharp.Net8.WPF
{
    public enum EditorInputType
    {
        Text,
        Boolean,
        Number,
        DateTime,
        Enum,
        FilePath,
        FolderPath,
        Object // 子物件
    }

    public enum PathType
    {
        File,
        Folder
    }
}
